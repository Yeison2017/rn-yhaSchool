import { StyleSheet } from "react-native";

import { SafeAreaLayout, Text } from "../../components";

const HomeScreen = () => {
  return (
    <SafeAreaLayout>
      <Text>HomeScreen1</Text>
    </SafeAreaLayout>
  );
};

export default HomeScreen;

const styles = StyleSheet.create({});
